# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# AI 工作记忆（planning-with-files 规划文件，不纳入仓库）
task_plan.md
findings.md
progress.md

# 参考资料：samples/ 与 st-source/（二进制/大文件/源码）不纳入仓库，
# 但 st-docs/ 下的文字文档与顶层 *.md 要经 git 同步（含 Lovable 产出）。
# git 无法在被忽略的目录内反选，故这里只忽略具体子内容、不忽略 _reference 目录本身。
_reference/samples/
_reference/st-source/
_reference/projects/

# bun 锁文件：本项目用 npm(package-lock.json)，bun.lock 由 Lovable 环境产生，不纳入仓库
bun.lock

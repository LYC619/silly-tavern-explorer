# 项目文件架构与整理规划（发布前 · 2026-07-27）

> 定位：本文是《发布规划-客户端首发.md》的配套文档。发布规划管"怎么发版"，本文管"发版前后项目文件怎么整理"。
> 来源：三路全项目盘点（src 源码 / 根目录文档与过程文件 / Tauri 与构建部署链）+ git 跟踪状态核对。
> 性质：**只规划，不执行**。所有"删除/归档/修改"均为建议，动手前按第二部分的时序走。

---

# 第一部分：文件架构与用途清单

## 1. 根目录总览

| 文件/目录 | 用途 | 状态判定 |
|---|---|---|
| `src/` | 前端源码（243 文件 / 约 37,800 行），详见 §2 | 核心 |
| `src-tauri/` | Tauri 客户端 Rust 侧，详见 §3 | 核心 |
| `public/` | 静态资源 + PWA 文件，详见 §4 | 核心（含遗留） |
| `dist/` | `npm run build` 产物（07-27 02:36），已 ignore | 构建产物 |
| `node_modules/` | 依赖，已 ignore | — |
| `README.md` | 项目门面。**内容停在 v0.18.0 旧四模块结构**，无 2.0 新架构、无客户端下载区，首行还挂 Vercel"推荐" | 过时，发版前重写 |
| `CHANGELOG.md` | 逐版本日志，最新 v0.18.0。**2.0 九阶段（35 个未 push commit）未写入** | 待补 1.0.0 |
| `package.json` | name `silly-tavern-explorer`、version 0.18.0、description "SillyTavern Chat **Beautifier**" | 描述是第 5 个产品名 |
| `index.html` | Vite 入口。标题 "SillyTavern Chat **Builder**"、PWA manifest link、og/twitter meta | 标题待统一 |
| `vite.config.ts` | 端口 8080、`@`→src 别名、排除 `_reference/`。**仍挂 lovable-tagger 插件（dev 模式）** | Lovable 遗留 |
| `vitest.config.ts` | jsdom + globals，只收 `src/**/*.{test,spec}` | 正常。但不在任何 tsconfig 工程内 |
| `tsconfig.json` | 根配置 `files:[]`+references。**其中宽松 compilerOptions 对 src 不生效**，是模板噪音 | 待清理 |
| `tsconfig.app.json` | 真正管 src/：`strict: true` | 正常 |
| `tsconfig.node.json` | 只管 vite.config.ts，**没含 vitest.config.ts** | 待补 |
| `eslint.config.js` | flat config；文件带 UTF-8 BOM | 待去 BOM |
| `tailwind.config.ts` | 主题/中文字体。content 含 `./pages/**` `./components/**` `./app/**` 三条 Next.js 风格无效路径 | 待清理 |
| `postcss.config.js` / `components.json` | 标准配置 / shadcn CLI 配置 | 正常 |
| `Dockerfile` / `nginx.conf` / `.dockerignore` | Web 部署路径（node 构建 → nginx 托管） | **保留**（网页版照旧已拍板）；nginx 有 sw.js 缓存 bug |
| `vercel.json` | SPA rewrite 一条 | 保留（同上） |
| `dev.bat` | Web 开发启动脚本（node 检查 → npm run dev） | 保留，建议补 Tauri 入口 |
| `start.bat` | Web 预览脚本，标题写着 "ST 对话美化器 **v0.2**" | 版本严重过时 |
| `.github/workflows/ci.yml` | 唯一 CI：lint → 双工程 tsc → vitest → build（Ubuntu/Node22，不含 Rust） | 正常，首推触发 |
| `.gitignore` | 含三件套/参考子目录/dist 等规则，注释写明设计意图 | 正常 |
| `task_plan.md` / `findings.md` / `progress.md` | planning-with-files 三件套（2.0 九阶段），已 ignore | 发版后归档 |
| `统一测试清单.md` | 阶段 1-7 全部待测项合并 + 客户端专项，**4~9 节未测完，仍在用** | 发版前置材料 |
| `二轮复测清单.md` | p9a-p9h 修复后的针对性复测清单，**当前最活跃** | 发版前置材料 |
| `待测试清单-阶段2.md` ~ `阶段6.md`（5 个） | 各阶段旧测试清单，**已被统一清单明文合并取代** | 可删/归档 |
| `_reference/` | 参考资料区，详见 §6 | — |
| `project_analysis_2026-07-15/` | 07-15 一次性审计产物，详见 §6 | 已过时 |
| `scripts/` | 28 文件 ~6MB：8 个 puppeteer 冒烟 `.cjs` + 18 张截图 + 日志，详见 §6 | 过程产物 |
| `.superpowers/` | brainstorming skill 运行时产物（2.0 首页布局草案），详见 §6 | 过程产物 |
| `.agents/` | **完全空目录**（git 不显示，容易被遗忘） | 可删 |

## 2. src/ 源码架构（243 文件 / 约 37,800 行）

> 无 `workers/`、无自建 Context——跨组件状态靠「父页持有 + props 下传 + localStorage/IndexedDB 持久化」。

### 2.1 根级文件

| 文件 | 行数 | 用途 |
|---|---|---|
| `main.tsx` | 9 | 入口：注册 SW + createRoot 挂载 |
| `App.tsx` | 67 | 路由表（13 条 lazy 路由）+ ThemeProvider/Tooltip/VaultGate 外壳 |
| `index.css` | 224 | 全局样式 + Tailwind 层 + 主题变量 |
| `App.css` | 42 | ⚠️ **死文件**：Vite 模板残留，零 import |
| `vite-env.d.ts` | 1 | Vite 类型声明 |

路由：`/`→Home，`/library`，`/character/:id`，`/story/:id`，`/tools`，`/chat`→Index，`/assets`，`/worldbook`，`/card-viewer`，`/preset`，`/regex`，`/settings`，`/ai-tools`→重定向 `/settings`，`*`→NotFound。

### 2.2 pages/（13 个页面，全部路由级代码分割）

| 文件 | 行数 | 用途 |
|---|---|---|
| `Home.tsx` | 308 | 首页"私人收藏馆"：角色库封面条+最近故事+处理区+其他资产，一屏不滚动 |
| `Library.tsx` | 551 | 角色库列表：排序/筛选/分级标签/角标/批量删除 |
| `CharacterPage.tsx` | 455 | 角色卡主页：简介+评分+故事列表+关联资产+立绘 |
| `StoryWorkspace.tsx` | 456 | 故事工作区：左二级栏切"阅读与编辑/整理与记录/导入与导出" |
| `Index.tsx` | 425 | `/chat` 聊天处理页（未绑定模式工作台）。⚠️ 文件名 Index 名不副实（历史遗留） |
| `Tools.tsx` | 198 | 处理区入口：大拖放区+按文件类型分流 |
| `WorldBook.tsx` | **1240** | 世界书编辑器主页——**全项目最大文件**，唯一未按子域拆分的页面 |
| `Preset.tsx` | 513 | 预设编辑页 |
| `RegexTool.tsx` | 450 | 正则独立工具页 |
| `CardViewer.tsx` | 332 | 角色卡查看/编辑（PNG/JSON round-trip） |
| `AssetLibrary.tsx` | 256 | 资产库页（世界书/预设/正则三类收藏） |
| `SettingsPage.tsx` | 54 | 设置页外壳（AI 配置卡+数据与存储面板） |
| `NotFound.tsx` | 24 | 404 |

### 2.3 lib/（66 个，纯逻辑层：纯函数、不触网、不碰 UI）

**adapters/st/**（ST 适配器边界，3 个）：`index.ts`(26,barrel)、`chat-jsonl.ts`(148,JSONL 解析/序列化+奇葩日期)、`reimport-merge.ts`(125,重复导入合并规则)。

**repo/**（存储仓库层，2 个）：`index.ts`(23,createRepo 惰性代理：vault 或 IndexedDB)、`idb-repo.ts`(68,泛型 IndexedDB CRUD 工厂)。

**vault/**（客户端明文文件库，11 个，Tauri 专属）：

| 文件 | 行数 | 用途 |
|---|---|---|
| `vault-backend.ts` | 611 | store→明文文件映射策略层（角色/故事/资产/临时目录布局） |
| `st-import.ts` | 379 | 首次接入 ST：扫描→勾选→复制进库并记 sourcePath |
| `fs.ts` | 206 | VaultFs 抽象接口 + createMemFs（测试用） |
| `st-writeback.ts` | 80 | 写回 ST：自动备份 .ste/ 保留 5 版 |
| `tauri-fs.ts` | 78 | Tauri 实现 + isTauri()（**唯一 import @tauri-apps/\* 的文件**） |
| `frontmatter.ts` | 60 | MD 记录无损 frontmatter 读写 |
| `st-update.ts` | 41 | 检查 ST 更新（比楼层数→合并） |
| `bootstrap.ts` | 38 | 客户端启动接线 |
| `sensitive-config.ts` | 35 | API Key 镜像到系统配置目录（防网盘泄漏） |
| `naming.ts` | 31 | 标题→安全文件名 |
| `active.ts` | 16 | 激活 vault 模块态开关 |

**lib/ 顶层——数据层**（`*-db.ts` 全为 createRepo 薄封装）：`archive-db.ts`(206,角色库+归档故事)、`idb.ts`(110,唯一连接层 DB_VERSION=9)、`summary-db.ts`(51)、`regex-db.ts`(55)、`card-db.ts`/`preset-db.ts`/`story-tree-db.ts`/`worldbook-db.ts`(各28)、`rating-db.ts`(25)、`storage-utils.ts`(331,整库备份/恢复/清理)、`session-storage.ts`(255,localStorage 跨页临时态)。

**解析与格式互通**：`png-parser.ts`(227,卡 V1/V2/V3 解析)、`png-writer.ts`(142,手写 PNG chunk+CRC32)、`card-export.ts`(92,round-trip 导出)、`card-embedded-assets.ts`(65,导卡提取内嵌资产)、`character-book.ts`(93)、`preset-parser.ts`(244,ST 预设无损回写)、`st-regex-interop.ts`(128,正则无损互导)、`txt-import.ts`(71)、`file-type-guess.ts`(88)。

**处理引擎**：`regex-processor.ts`(249)、`regex-preview.ts`(96)、`summary-engine.ts`(318,对齐 ST 上下文语义)、`summary-templates.ts`(198)、`novel-view.ts`(271,小说视图三层管道)、`character-ai.ts`(264)、`story-tree-ai.ts`(291,LLM JSON ops 幂等 apply)、`story-tree-model.ts`(178)、`story-tree-io.ts`(75)、`mini-summary.ts`(75)、`diary-parser.ts`(84)、`chat-edit.ts`(74,楼层/swipe 纯函数)、`story-meta.ts`(103,模型名+游玩时长)、`organize-index.ts`(141,整理左栏统一索引)、`bind-story.ts`(61)、`asset-cow.ts`(96,资产写时复制)、`worldbook-entry-copy.ts`(24)、`tag-taxonomy.ts`(60,分级标签)、`rating-templates.ts`(52)。

**导出与杂项**：`share-image.ts`(320,canvas 分享长图)、`obsidian-export.ts`(111)、`svg-export.ts`(59)、`tour-steps.ts`(131,引导步骤常量)、`register-sw.ts`(76,SW 注册/Tauri 下注销)、`tool-handoff.ts`(28)、`st-ai-config.ts`(64,只读 ST settings.json，绝不读密钥)、`utils.ts`(6,cn())。

### 2.4 components/（106 个，13 个子域）

**顶层（7 个）**：`AppLayout.tsx`(123,全局布局+双端槽位)、`GlobalSettings.tsx`(459,数据与存储面板，含 APP_VERSION)、`GuidedTour.tsx`(372)、`CharacterCardViewer.tsx`(266,⚠️ 文件名 Viewer 实际导出 CharacterCardEditor)、`MarkdownLite.tsx`(103,React 元素渲染天然防 XSS)、`DemoData.tsx`(88)、`HelpCard.tsx`(26,被 8 页复用)。

**ui/**（shadcn 30 个，约 1650 行）：未做业务改造的标准生成物。高频 button(69)/input(36)/label(32)/badge(28)/card(25)/dialog(24)/select(23)。⚠️ **零引用 4 个**：`alert.tsx`、`table.tsx`、`toggle-group.tsx`、`use-toast.ts`(re-export shim)。

**chat/**（13 个，约 3850 行）：`ChatWorkbench.tsx`(550,统一工作台)、`ChatPreview.tsx`(678,虚拟化楼层预览)、`RegexSidebar.tsx`(591)、`ExportButton.tsx`(396,四格式导出)、`ChatImporter.tsx`(348)、`RegexQuickAdd.tsx`(301)、`MessageNavBar.tsx`(180)、`SettingsPanel.tsx`(168)、`ChapterMarkerDialog.tsx`(130)、`MessageEditDialog.tsx`(123)、`EditorToolbar.tsx`(111)、`BindStoryDialog.tsx`(101)、`MessageSearchBar.tsx`(50)。

**organize/**（5 个，约 1700 行）：`OrganizePanel.tsx`(477,三栏容器)、`RecordWorkbench.tsx`(550,流式生成工作台)、`TreeWorkbench.tsx`(401,四视图+拖拽+撤销)、`ContextRail.tsx`(197)、`ShareImageDialog.tsx`(77)。

**summary/**（8 个，约 1250 行）：`BatchProcessor.tsx`(320)、`TemplatePicker.tsx`(185)、`FloorRangePicker.tsx`(171)、`AttachPanel.tsx`(169,含 token 估算)、`SummaryResultEditor.tsx`(140)、`MiniSummaryPanel.tsx`(108)、`DiaryView.tsx`(100)、`PriorVolumesPanel.tsx`(56)。

**worldbook/**（10 个，约 2050 行）：`EntryEditor.tsx`(336)、`BatchOperations.tsx`(329)、`QuickCreate.tsx`(327)、`AIUpdateDialog.tsx`(267)、`PrefixCategorize.tsx`(243)、`AIRewriteContent.tsx`(212)、`EntryCard.tsx`(142)、`WorldBookImporter.tsx`(96)、`EntryListRow.tsx`(73)、`WorldBookExporter.tsx`(29)。

**story-tree/**（6 个，约 1180 行）：`AIFillDialog.tsx`(351)、`StoryMindmap.tsx`(321,样式内联保证可导出)、`StoryTreeView.tsx`(142,三落点拖拽)、`StoryNodeEditor.tsx`(142)、`StoryCardView.tsx`(122)、`StoryTimeline.tsx`(99)。

**character/**（4 个，约 1200 行）：`RatingPanel.tsx`(526,手动/模板加权/AI 三种)、`IntroSection.tsx`(338,AI 简介+版本历史)、`AssetSection.tsx`(266,共享/派生+写时复制入口)、`IllustrationSection.tsx`(73,仅客户端)。

**ai-tools/**（7 个+barrel，约 740 行）：`useApiProfiles.ts`(193)、`useOpenAI.ts`(180,SSE 流式)、`APIConfigCard.tsx`(146,全应用唯一配置卡)、`api-profiles.ts`(103)、`ModelNameInput.tsx`(67)、`ProfileToolbar.tsx`(48)、`ApiStatusLine.tsx`(46)、`index.ts`(4)。

**preset/**（6 个，约 860 行）：`PromptEditor.tsx`(371)、`PresetExport.tsx`(161)、`PresetRegexEditor.tsx`(141)、`PresetOverview.tsx`(67)、`PresetRoleBadge.tsx`(65)、`PresetUtilityFields.tsx`(54)。

**workspace/**（5 个，约 730 行）：`IOPanel.tsx`(272,检查更新/导出/写回三区)、`BranchPanel.tsx`(161)、`WritebackSection.tsx`(133)、`OutlinePanel.tsx`(85)、`STUpdateHint.tsx`(82)。

**reader/**（2 个，约 1000 行）：`NovelView.tsx`(546)、`ReaderView.tsx`(454)。⚠️ 功能相邻的一对（都是阅读覆盖层），建议人工确认是否重叠。

**tools/ 与 vault/**（3 个）：`STImportCard.tsx`(298,接入 ST 卡片)、`STAIConfigDialog.tsx`(110,ST 配置只读快照)、`VaultGate.tsx`(52,文件库门卫)。

### 2.5 hooks/ types/ test/

**hooks/**（2 个）：`use-toast.ts`(186,被 38 文件用)、`use-mobile.tsx`(19,仅 2 处用)。

**types/**（8 个，约 940 行）：`chat.ts`(104,被 64 文件引用)、`archive.ts`(178,2.0 核心实体)、`worldbook.ts`(280,含解析归一化)、`summary.ts`(90)、`story-tree.ts`(93)、`preset.ts`(109,未知字段存 originalData)、`rating.ts`(62)、`character-card.ts`(23)。

**test/**（45 个，约 5180 行）：44 个测试文件 + setup.ts，全部集中此目录。覆盖重心是 lib/ 纯函数与 types/ 解析器；components/pages 基本无测试（唯一例外 markdown-lite.test.tsx）。`version-sync.test.ts` 校验版本号一致性。vitest 当前 449 全绿。

## 3. src-tauri/（Tauri 客户端）

| 项 | 现状 |
|---|---|
| `tauri.conf.json` | productName/binary=`STE`，identifier=`com.ste.explorer`，version=**0.18.0**，窗口 1440×900 标题 `SillyTavern Explorer`，`csp: null`，bundle 仅 `nsis`（icon 列表含无用的 icns），无 updater/签名/publisher |
| `Cargo.toml` | tauri 2.11.3 + log/dialog 插件 + serde/base64。**元数据全是模板占位**：name="app"、version="0.1.0"、description="A Tauri App"、authors=["you"]、license 空 |
| `src/lib.rs` | 约 300 行全部业务：12 个 vault_* command（原子写入、拒绝覆盖、只删点名单文件/空目录、绝不递归）+ config 读写。**含 6 个单测但无任何 CI 在跑** |
| `src/main.rs` / `build.rs` | 模板标准入口 |
| `capabilities/default.json` | 仅 `core:default`+`dialog:allow-open`，最小权限；路径校验责任全在 TS 层 |
| `icons/`（16 个） | ⚠️ **全是 Tauri 官方模板默认图标（黄青 logo），未换品牌图标——发布阻塞项** |
| `gen/schemas/` | 自动生成，已 ignore |

## 4. public/

| 文件 | 状态 |
|---|---|
| `sw.js` | CACHE_VERSION='v0.18.0'。Tauri 下由 register-sw.ts 注销规避（commit ff71b88），Web 端照常。会被打进 exe 内嵌资产（2.7KB 无害噪音） |
| `manifest.json` | PWA manifest，名称 "**ST 聊天记录处理器**"（又一个产品名） |
| `icon-192/512.png`、`favicon.ico` | PWA 图标集（与 Tauri icons/ 是两套） |
| `minimal.html` | 独立单文件版工具（README 引导下载的分发物） |
| `placeholder.svg` | Lovable 模板遗留，**已核实零引用** |
| `robots.txt` | 网页版用，保留 |

## 5. 三条构建/发布路径

| 路径 | 入口 | 状态 |
|---|---|---|
| Tauri 客户端 | `npm run tauri -- build`（无快捷 script）→ 内嵌 dist → NSIS | **首发主路径**，纯手工出包，无 release workflow |
| Docker | Dockerfile → nginx | 保留（网页版照旧）。⚠️ nginx.conf 静态缓存正则命中 `/sw.js` 打 1 年 immutable，会卡死 SW 更新——真 bug |
| Vercel | 平台自动 build + vercel.json rewrite | 保留。README 仍标"推荐"与客户端首发矛盾 |

CI（ci.yml）只做质量门禁（lint/tsc/vitest/build），不出任何发布产物，Rust 测试不在其中。

## 6. 文档与开发过程文件

**_reference/**：
- `_README_放置说明.md` — 目录规则说明【保留，顶部 Lovable 事故说明可精简】
- `发布规划-客户端首发.md`（07-27）— 发版执行蓝图【当前最重要，untracked】
- `Lovable提示词.md` — 生成 st-docs 的历史提示词【任务已完成，可归档】
- `头脑风暴.md`（30KB）— ⚠️ **内容与 STE 无关**（math-reader-boox 项目推广咨询记录），文件名有误导性
- `STE 概念设计/`（untracked）— **2.0 设计定稿**（明文声明"规格排期唯一依据"）+ 界面草图 + `2.0草图存档/`【核心资产，应入库】
- `st-docs/`（01~04，已入库）— ST 四种格式契约文档【长期保留】
- 已 ignore：`samples/`(测试素材)、`summary/`(总结参考+Memory Wizard 源码)、`_archived-deadcode/`、`计划归档/2026-07-18-v0.17.0/`(既有归档先例)、`st-source/`(**空目录**)

**project_analysis_2026-07-15/**：一次性审计。报告点名的五项欠账已在 v0.18.0 全部修复，审计对象还是 2.0 前架构【整体已过时】。`final_report.md` 已入库，三件套已 ignore，`review-build/`（约 60 个 hash 文件）纯占体积。

**scripts/**（untracked）：8 个 puppeteer 冒烟 `.cjs`（种子 IndexedDB+截图断言，**硬编码本机 Edge 路径**，package.json 无引用）+ 18 张 `shot-*.png`（约 5.8MB 一次性证据）+ 日志。脚本有复用价值，截图是垃圾体积。

**.superpowers/**（untracked）：brainstorm 会话产物（2.0 首页布局 HTML/SVG 草案），与 `2.0草图存档/` 高度重叠且更原始。

**.agents/**：空目录。

## 7. git 跟踪状态视图

- 本地 main 领先 origin/main **35 个 commit**（v0.17.1 → 阶段 0-9），未 push。
- 已跟踪文档仅 9 个 .md：README、CHANGELOG、Lovable提示词、_README_放置说明、st-docs×4、final_report。
- **untracked 12 项**（push/收版时的决策点）：`.superpowers/`、`scripts/`、`_reference/STE 概念设计/`、`_reference/发布规划-客户端首发.md`、`_reference/头脑风暴.md`、7 个测试清单。
- 已 ignore 而容易漏盘的：三件套×2 处、review-build/、_archived-deadcode/、samples/、summary/、st-source/、各 *.log。
- `.agents/` 空目录既不在 status 也不在 ignore——最容易被彻底遗忘的残留。

---

# 第二部分：发布前整理规划

## 0. 前提与原则

1. **网页版保留已拍板**（发布规划："Vercel/Docker 跟 main 走"）→ Dockerfile/vercel.json/nginx.conf/sw.js/manifest.json/robots.txt **都不删**，只修其中的 bug。
2. **正在使用的测试清单不动**：统一测试清单 + 二轮复测清单是发版前置材料，测完才能归档。
3. 整理动作分四类——**删除 / 归档 / 修改 / 决策**，每项都标了执行时机，与发布五步对齐。
4. src 内的删改必须过门禁：vitest 449 全绿 + `tsc -p tsconfig.app.json --noEmit` 0 错 + lint 0 error + build 成功。

## 1. 发版阻塞项（收版 commit v1.0.0 前必须做）

| # | 事项 | 详情 |
|---|---|---|
| 1 | **品牌图标** | `src-tauri/icons/` 16 个全是 Tauri 模板默认 logo。需自制源图 → `npx tauri icon` 生成全套；顺带决定 `public/` 的 PWA 图标（favicon/icon-192/512）是否同步换 |
| 2 | **产品名统一（5 处不一致）** | `STE`（binary/productName）/ `SillyTavern Explorer`（窗口标题）/ `SillyTavern Chat Builder`（index.html title，任务栏可见）/ `ST 聊天记录处理器`（manifest.json + 页脚）/ `SillyTavern Chat Beautifier`（package.json description）。拍板一个主名（建议 STE / SillyTavern Explorer 主副搭配），全部改齐 |
| 3 | **版本号四处同步 + 补测试缺口** | 改 package.json / APP_VERSION / sw.js / tauri.conf.json 至 1.0.0。⚠️ `version-sync.test.ts` 只钉前三处，**tauri.conf.json 不在校验内**——建议把它加进测试，否则以后 exe 版本会静默漂移 |
| 4 | **Cargo.toml 元数据** | name="app"/version="0.1.0"/description="A Tauri App"/authors=["you"]/license 空 → 全部填真实值 |
| 5 | **README 重写** | 现内容停在 v0.18.0 旧四模块，且把 Vercel 标"推荐"、全文无 Tauri/exe 字样。按发布规划第五步重写：2.0 架构 + 客户端下载区 + SmartScreen 说明 + 网页版差异 |
| 6 | **CHANGELOG 补 1.0.0** | 2.0 九阶段 35 个 commit 的变化按用户视角写入 |

## 2. 建议顺手做（清理 commit，低风险小修）

- `nginx.conf`：静态缓存正则排除 `/sw.js`（否则网页版 SW 更新被 1 年 immutable 卡死）
- `tailwind.config.ts`：删 `./pages/**`、`./components/**`、`./app/**` 三条无效 content 路径
- 根 `tsconfig.json`：删不生效的宽松 compilerOptions（`strictNullChecks:false` 等），避免与 app 工程 strict 误读
- `tsconfig.node.json`：include 补 `vitest.config.ts`
- `eslint.config.js`：去 UTF-8 BOM
- `start.bat`：标题 "ST 对话美化器 v0.2" 更新为当前产品名+版本（或干脆注明仅 web 预览用）
- `dev.bat`：补 Tauri 开发入口（`npm run tauri dev`）或新增 tauri-dev.bat；package.json 顺带加 `tauri:dev`/`tauri:build` 快捷 script
- `tauri.conf.json`：bundle icon 列表删无用的 `icon.icns`（无 macOS 目标）；`csp: null` 属有意决策，在 conf 旁注释或文档记录一句即可

## 3. 删除清单（零引用已核实）

**src 内（约 209 行，进清理 commit）**：
- `src/App.css`（Vite 模板残留，零 import）
- `src/components/ui/alert.tsx`、`table.tsx`、`toggle-group.tsx`、`use-toast.ts`（shadcn 零引用；将来要用 CLI 一条命令能再生成）
- 无引用导出：`tour-steps.ts` 的 `AITOOLS_TOUR_STEPS`（/ai-tools 页并入设置后的明确遗留）、`png-parser.ts#getCharacterDescription`、`rating-db.ts#getRatingTemplate`、`rating-templates.ts#isBuiltinRatingTemplate`、`session-storage.ts#getMergedRegexRules`、`story-tree-db.ts#getStoryTree`/`pruneAutoSavedStoryTrees`、`summary-db.ts#getSummary`
- ⚠️ 不要动："仅被测试引用"的约 20 个导出（createMemFs 等，可测试性拆分）；`idb.ts` 的 `'books'` store、`tour-steps.ts` 旧 key、`storage-utils.ts` 忽略旧 books、`/ai-tools` 重定向——都是有注释的历史兼容项

**工作区（不需要 commit，直接删）**：
- `public/placeholder.svg`（零引用，这个在 git 里，删除进清理 commit）
- `scripts/shot-*.png` 18 张（约 5.8MB 一次性截图证据）
- `.agents/` 空目录
- `_reference/st-source/` 空目录
- `project_analysis_2026-07-15/review-build/`（约 60 文件纯体积，本就 ignore）
- `待测试清单-阶段2~6.md` 5 个（已被统一清单合并；不放心就并入归档）
- `.superpowers/`（brainstorm 产物；content/ 4 个 HTML/SVG 若想留就并进 `_reference/STE 概念设计/2.0草图存档/`）

## 4. 归档清单（发版后，照 `_reference/计划归档/2026-07-18-v0.17.0/` 先例）

新建 `_reference/计划归档/2026-07-27-v1.0.0/`（或按实际发版日）收入：
- 根目录三件套 `task_plan.md` / `findings.md` / `progress.md`（progress 是写发布说明的素材，发布说明写完再归）——归档前把 findings.md 里的"ST JSONL 字段硬约束"抽进 `_reference/st-docs/`
- `统一测试清单.md` + `二轮复测清单.md`（复测全部通过后）
- `project_analysis_2026-07-15/` 整目录移入（`final_report.md` 保留在库里或一并归档，二选一）
- `_reference/Lovable提示词.md`（任务已完成）
- `_reference/发布规划-客户端首发.md` + 本文（发版落地后）

## 5. untracked 12 项处置表（第一步 push 前过一遍）

| 项 | 建议 |
|---|---|
| `_reference/STE 概念设计/` | **add 入库**——2.0 设计定稿是"规格唯一依据"，符合"顶层 _reference 文档入 git"的既定意图（`2.0草图存档/` 体积不大可一并入，或加 ignore） |
| `_reference/发布规划-客户端首发.md` | add 入库（发版后再归档） |
| `_reference/头脑风暴.md` | 与 STE 无关（是 math-reader-boox 推广咨询）→ 移出项目，或改名 `参考-开源推广经验.md` 后加 ignore |
| `scripts/*.cjs` 8 个 | 二选一：a) 加 `.gitignore`（scripts/ 整目录）本地留用；b) 想入库先把硬编码 Edge 路径改成环境变量。推荐 a——阶段 9 后价值递减 |
| `scripts/shot-*.png` | 删（见 §3） |
| `.superpowers/` | 删或加 ignore（见 §3） |
| 7 个测试清单 | 现阶段留在工作区用；给 `.gitignore` 加规则（如 `*测试清单*.md`、`*复测清单*.md`）避免误入库，测完归档 |

## 6. 决策点（2026-07-29 已全部拍板）

1. **对外产品名 = `ST-Explore`**——后续兼容其他 AI RP 客户端时不用改名。收版 commit 时统一改 7 处（tauri.conf productName/binary、窗口标题、index.html title、manifest.json、package.json name+description、README、发布说明）
2. **scripts/ 冒烟脚本**：本地留用 + 加 .gitignore（硬编码 Edge 路径不适合入库）✅ 已执行
3. **头脑风暴.md**：移出项目 → `D:\8.Project\0_0_维护\参考-开源推广经验(math-reader-boox).md` ✅ 已执行
4. **ReaderView / NovelView**：暂时保留双轨（有开发潜力），两文件头部已加标记注释 ✅ 已执行
5. **Lovable 痕迹**：立即清除（vite 插件 + lovable-tagger 依赖 + placeholder.svg）✅ 已执行
6. `minimal.html` 独立单文件版：README 重写时决定是否继续作为分发物宣传（唯一未决项）

## 整理执行记录（2026-07-29）

按 §8 时序提前执行了"push 前 + 清理 commit"两档动作（用户指示），**删除均入回收站待用户确认**：

- 回收站（34 项）：`.agents/`、`_reference/st-source/`、`project_analysis_2026-07-15/review-build/`、`.superpowers/`、`scripts/shot-*.png`×18 + vite-dev.log、`待测试清单-阶段2~6.md`×5、`public/placeholder.svg`、`src/App.css`、`ui/alert.tsx`、`ui/table.tsx`、`ui/toggle-group.tsx`、`ui/use-toast.ts`
- 移出：头脑风暴.md（见上）
- src 删 8 个无引用导出（删前逐一 grep 复核零引用）：AITOOLS_TOUR_STEPS、getCharacterDescription、getRatingTemplate、isBuiltinRatingTemplate、getMergedRegexRules、getStoryTree、pruneAutoSavedStoryTrees、getSummary
- 依赖清理：lovable-tagger、@radix-ui/react-toggle-group（随 toggle-group.tsx 孤儿化）、@tailwindcss/typography（本就零引用），lockfile 已同步
- 配置修复：nginx.conf 给 `/sw.js` 加 no-cache 精确匹配（修 SW 更新被 1 年 immutable 卡死的 bug）、tailwind 删 3 条无效 content 路径+补 index.html、根 tsconfig 只留 paths、tsconfig.node 补 vitest.config.ts、eslint.config.js 去 BOM、tauri.conf 删 icon.icns、start.bat/dev.bat 标题改 ST-Explore
- 新增：`tauri-dev.bat` + package.json `tauri:dev`/`tauri:build` 快捷 script
- git：.gitignore 加 scripts//.superpowers//测试清单规则；add 入库《STE 概念设计》《发布规划》《本文》
- 补充（07-29 二轮）：`project_analysis_2026-07-15/` 整目录移入 `_reference/计划归档/2026-07-15-项目审计/`（final_report.md 走 git mv 保留跟踪，三件套按惯例本地归档），.gitignore 删除失效的 review-build 规则
- **未 commit**：等用户确认回收站内容无影响后一并提交

## 7. 远期 / 本轮不动

- `WorldBook.tsx`(1240 行) 拆分——唯一未按子域拆的页面，重构首选；次级大文件 ChatPreview(678)/vault-backend(611)/RegexSidebar(591) 等
- `Index.tsx` 改名（/chat 页叫 Index 易误读）、`CharacterCardViewer.tsx` → 与导出名 CharacterCardEditor 对齐
- `tauri-fs.ts` 动态 import 隔离（web bundle 里带着永不调用的 Tauri 模块）
- Rust 6 个单测接入 CI（`cargo test`），可与远期 release workflow（tauri-action tag 出包）一起搭
- 自动更新 updater、代码签名——发布规划已列远期

## 8. 执行时序（与发布五步对齐）

| 发布规划步骤 | 本文对应动作 |
|---|---|
| **第一步 push 35 commit 前** | §5 untracked 处置表过一遍（该 add 的 add、该 ignore 的 ignore、头脑风暴移出）；工作区垃圾（§3 下半）随手删，不占 commit |
| **第二步 真机复测** | 期间不动 src（避免测试目标漂移） |
| **复测通过后 → 收版前** | 清理 commit：§3 src 内删除 + §2 顺手小修，跑满门禁（vitest/tsc/lint/build） |
| **第三步 收版 commit** | §1 阻塞项 1-6（图标/产品名/版本四处+测试补缺/Cargo 元数据/README/CHANGELOG），然后 `npm run tauri build` 出正式包 |
| **第四步 tag+Release / 第五步 README** | 照发布规划执行 |
| **发版后** | §4 归档清单执行；§6-4/5、§7 排期 |

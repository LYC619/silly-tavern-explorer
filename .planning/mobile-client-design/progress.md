# 移动端适配进度

## 已完成

### 上一轮：外壳与端点（3845bf5 … 141aded，6 commit）

- 底部标签栏 + 左侧抽屉，边缘手势开合
- 小说视图的移动端阅读体验（滚动/翻页两模式、沉浸工具栏、三分区手势）
- 首页 / 角色库 / 角色页 / 设置页响应式
- 加载状态：首屏骨架、刷新指示条、失败态
- 贴底浮层避开标签栏（`--mobile-tab-bar-h`）

留下的缺口：适配了**端点**（小说视图）却没适配**通道**——从首页走到小说视图这一路上，
故事工作区和编辑区在 375px 下会横向溢出。

### 本轮：打通阅读通道 + 移动端架构基础（75dc67c … 9fc91ef，5 commit）

| commit | 内容 |
|---|---|
| `75dc67c` | react-refresh 警告基线归零（10 条真警告 + 9 条来自陈旧 worktree 的幻影） |
| `82a018a` | 故事工作区窄屏布局：宽二级栏拆进左右两个抽屉 |
| `4aa6d03` | 编辑区窄屏形态：工具栏收纳进「更多」，正则栏改为下方展开 |
| `4a69763` | 通道剩余问题：就地阅读顶栏单行化、工具条排序、悬停显形改 `(hover: hover)` |
| `f687702` | 小说视图段落虚拟化 + 修掉分页容量塌到下限的根因 |
| `9fc91ef` | Capacitor Android 外壳脚手架 + 三档运行环境抽象 |

门禁：150 测试文件 / 1252 用例全绿，两个 tsconfig 工程 tsc 干净，eslint 零警告。
桌面档（≥1024px）零改动——所有移动端改动都是新增的 `isCompact` / `isMobile` 分支，
jsdom 默认 1024px 落桌面档，既有用例全部继续走适配前的路径。

### 本轮（2026-09-01）：移动端可用性 + 发版准备（e80147d … c67b8f6，6 commit）

| commit | 内容 |
|---|---|
| `e80147d` | 清理 .worktrees 残留，eslint 忽略规则改指 .claude/worktrees |
| `2884c60` | 阅读包格式 + DataSource 抽象（三通道接口：reading-pack / cloud-sync / st-backup-zip） |
| `dc23d31` | 阅读包导出与导入界面（角色页导出、设置页导入，三方落盘适配） |
| `e32828e` | Android 返回键——按层分发（Radix 浮层 → 注册层 → 沉浸阅读 → 抽屉 → 路由兜底） |
| `5d1df6e` | 分享图生成——Canvas 绘制卡片，Capacitor Share 调起系统分享面板 |
| `c67b8f6` | vite 5.4.21 → 8.2.2，解决 esbuild 安全漏洞 |

门禁：152 测试文件 / 1282 用例全绿，tsc 干净，eslint 零警告。
桌面端（≥1024px）零改动——所有新增功能都门控在 Capacitor 环境或 compact 档。

## 未完成（本轮未纳入）

- **世界书移动端适配**：条目列表 + 插入策略字段双栏结构需拆成两级，字段分组折叠。
  属于重排信息层级，不是调 className，需单独排期。已合并 `useIsMobile` 到 `useViewport`。
- **故事树移动端适配**：导图/时间轴二维画布需改为「选节点→查看详情」两级，拖拽改长按+移动。
- **AI 面板移动端适配**（BatchProcessor / AIUpdateDialog / AIFillDialog）：
  「提示词 + 进度 + 结果」并置需改为分步或抽屉，且要考虑挂后台恢复。
- **图标与启动图**：仍是 Capacitor 默认占位。
- **「到电脑上做」提示**：后排功能入口未加轻提示。
- **真机实测**：本轮改动只在 jsdom + devtools 窄屏验证，未在真设备上跑。

上述三个适配项在 `docs/mobile-adaptation.md` 标注为"需要重排信息层级或重新设计交互，
不是调 className 能解决的。本轮的口径是只动布局和交互壳层，不动业务逻辑"，故未纳入。

## 下一步建议顺序

1. 真机实测（打 debug APK，验证阅读包导入 → 阅读 → 分享图 → 返回键全流程）
2. 图标与启动图（替换 Capacitor 默认占位）
3. 世界书 / 故事树 / AI 面板适配（每个单独排期，非临近发版阻塞项）
4. 「到电脑上做」提示（后排功能入口加轻提示）
